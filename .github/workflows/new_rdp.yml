name: "🗞 New GeoRDP"

on:
  workflow_dispatch:
    inputs:
      date:
        type: string
        description: Date de la prochaine RDP au format YYYY-MM-DD
        required: true
      notify-slack:
        type: boolean
        description: Notifie l'équipe sur Slack

jobs:
  create_rdp:
    runs-on: ubuntu-latest

    steps:
      - name: Get source code
        uses: actions/checkout@v2

      - name: Create a new GeoRDP
        shell: bash
        run: |
          # make file runnable, might not be necessary
          # chmod +x ./tools/new_rdp.sh

          # run script
          bash ./tools/new_rdp.sh ${{ github.event.inputs.date }}

      # - name: New branch
      #   run: |
      #     git config user.name ${{ github.actor }}
      #     git config user.email 'geotribu@gmail.com'
      #     git checkout -b rdp/${{ github.event.inputs.date }}
      #     git status

      # - name: Commit new GeoRDP
      #   run: |
      #     git add content/rdp/
      #     git commit -am "🗞 Crée la GeoRDP du ${{ github.event.inputs.date }}"

      # - name: Push to remote
      #   run: git push origin rdp/${{ github.event.inputs.date }}

      - name: Create Pull Request
        id: cpr
        uses: peter-evans/create-pull-request@v3
        with:
          commit-message: "🗞 Crée la GeoRDP du ${{ github.event.inputs.date }}"
          committer: ${{ github.actor }} <${{ github.actor }}@users.noreply.github.com>
          delete-branch: true
          draft: true

      - name: Notify Slack
        run: |
          echo "Notification sur Slack : Pull Request URL - ${{ steps.cpr.outputs.pull-request-url }}"
